apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
  labels:
    app: mariadb
    type: database
spec:
  selector:
    app: mariadb
    type: database
  ports:
  - port: 3306
    targetPort: db-port
  type: ClusterIP 